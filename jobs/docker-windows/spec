---
name: docker-windows

templates:
  pre-start.ps1: bin/pre-start.ps1
  config/daemon.json.erb: config/daemon.json
  monit.erb: monit

packages:
- docker-windows

properties: {}

consumes:
- name: docker
  type: docker-tcp
